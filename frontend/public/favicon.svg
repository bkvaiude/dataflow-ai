<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" width="32" height="32">
  <!-- 6 outer dots in hexagon pattern -->
  <!-- Right (0deg) -->
  <circle cx="26" cy="16" r="3" fill="#3b82f6"/>
  <!-- Top-right (300deg) -->
  <circle cx="21" cy="7.34" r="3" fill="#3b82f6"/>
  <!-- Top-left (240deg) -->
  <circle cx="11" cy="7.34" r="3" fill="#3b82f6"/>
  <!-- Left (180deg) -->
  <circle cx="6" cy="16" r="3" fill="#3b82f6"/>
  <!-- Bottom-left (120deg) -->
  <circle cx="11" cy="24.66" r="3" fill="#3b82f6"/>
  <!-- Bottom-right (60deg) -->
  <circle cx="21" cy="24.66" r="3" fill="#3b82f6"/>

  <!-- Center bullseye - concentric rings -->
  <!-- Outer ring -->
  <circle cx="16" cy="16" r="5" fill="#1e40af"/>
  <!-- Middle ring -->
  <circle cx="16" cy="16" r="3.5" fill="#3b82f6"/>
  <!-- Inner ring -->
  <circle cx="16" cy="16" r="2" fill="#1e40af"/>
  <!-- Center dot (bullseye) -->
  <circle cx="16" cy="16" r="0.8" fill="#ffffff"/>
</svg>
